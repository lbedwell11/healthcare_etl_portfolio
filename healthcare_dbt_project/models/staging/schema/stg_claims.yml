
version: 2

models:
  - name: stg_claims
    description: Staging model for claims data
    columns:
      - name: claim_id
        description: Unique key for this table
        data_tests:
          - unique
          - not_null
      - name: member_id
      - name: provider_id
      - name: service_date
      - name: billed_amount
      - name: paid_amount
      - name: claim_status
