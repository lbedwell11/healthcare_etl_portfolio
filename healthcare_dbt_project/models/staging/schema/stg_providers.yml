
version: 2

models:
  - name: stg_providers
    description: Staging model for provider data
    columns:
      - name: provider_id
        description: Unique key for this table
        data_tests:
          - unique
          - not_null
      - name: provider_name
      - name: specialty
